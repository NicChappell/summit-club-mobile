import { TextStyle, ViewStyle } from "react-native";
import { paddingReset } from "./reset";

export const button = {} as ViewStyle;

export const buttonContainer = {} as ViewStyle;

export const buttonTitle = {} as TextStyle;
